machine:
  services:
    - docker

dependencies:
  override:
    - docker pull migurski/gdalambda || true

test:
  override:
    - docker run migurski/gdalambda python -c "from osgeo import ogr; geom = ogr.CreateGeometryFromWkt('point(0 0)'); assert str(geom) == 'POINT (0 0)'"
